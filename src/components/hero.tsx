import { ThemeToggle } from "./theme-toggle";

const Hero = () => {
  return (
    <section id="about" className="h-full">
      about section
      <ThemeToggle />
    </section>
  );
};

export default Hero;
